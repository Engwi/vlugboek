# Vlugboek Next Steps

1. Replace synthetic parsing with real PDF table extraction.
2. Lock down report type recognition from PDF content, not only filenames.
3. Build the admin import confirmation screen.
4. Add proper Spring Security authentication, BCrypt password hashing, and guarded admin endpoints.
5. Improve searchable metadata for federation, club, loft, race point, category, and official dates.
6. Add ingestion tests using supplied PDFs as fixtures.
7. Add the production deployment path with repeatable build, upload, install, start, and health checks.
